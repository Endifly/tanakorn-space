import { Chip, TextField, Container } from "@mui/material";

const GrandChasePortal = () => {
  return (
    <Container style={{ padding: 16 }}>
      {/* <Box */}
      <TextField label="set" size="small" className="text-3xl" />
      <TextField label="stat" size="small" />
    </Container>
  );
};

export default GrandChasePortal;
